import { renderHook } from '@test';

import { use{{pascalCase name}}Controller } from '../{{kebabCase name}}.controller';

describe('{{pascalCase name}}Controller', () => {
  it('should be render hook correctly', () => {
    const { result } = renderHook(() => use{{pascalCase name}}Controller({}));

    expect(true).toBeTruthy();
  });
});
