import { render, screen } from '@test';

import { {{pascalCase name}}Controller } from '../{{kebabCase name}}.controller';
import { {{pascalCase name}}View } from '../{{kebabCase name}}.view';

const controller: {{pascalCase name}}Controller = {};

function customRender() {
  render(<{{pascalCase name}}View controller={controller} />);

  return {
    element: screen.getByTestId(''),
  };
}

describe('<{{pascalCase name}}View />', () => {
  it('should be render component correctly', () => {
    const {} = customRender();

    expect(true).toBeTruthy();
  });
});
