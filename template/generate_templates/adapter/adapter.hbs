import { {{pascalCase name}}Impl } from './types';

export let {{lowerCase name}}:{{pascalCase name}}Impl;

export function set{{pascalCase name}}Impl({{lowerCase name}}Impl: {{pascalCase name}}Impl) {
    {{lowerCase name}} = {{lowerCase name}}Impl;
}
